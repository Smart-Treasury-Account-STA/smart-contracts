{
  "name": "sta-sdk-workspace",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "packages/*",
    "generated/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.2"
  }
}
