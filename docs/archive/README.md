# Archive

Historical material from the earlier partial PoC revision (`smart_account_poc`, `policy_registry_poc`, `intent_registry_poc`, `recovery_guard_poc` on `soroban-sdk 22.0.1`), kept for traceability only.

**Nothing in this folder is part of the current implementation.** If you are reviewing the Smart Treasury Account V1 contracts, start at `../README.md` instead — everything here predates the current `contracts/` workspace, uses different contract names, and describes a different `soroban-sdk` version.

| File | What it is |
|---|---|
| `POC_TESTNET_DEPLOYMENT.md` | The PoC's testnet deployment record (contract IDs, WASM hashes) — superseded by `../TESTNET_DEPLOYMENT.md` |

The PoC's own scope/review-guide documents were rewritten in place rather than archived as separate files; their original content is available in this repository's git history (the commit that renamed `*_poc` packages and their docs to the current V1 versions).
